<script lang="ts">
  // Verbatim copy of ui/src/lib/components/ndo/CompositionTab.svelte.
  // The app's file is markup-only; this empty script block exists so the
  // module has a type declaration for its importer, and changes nothing that
  // renders.
</script>

<div class="rounded-lg border border-dashed border-gray-400 bg-white p-6">
  <span class="mb-3 inline-block rounded bg-amber-50 px-2 py-0.5 text-xs text-amber-600">Coming soon</span>
  <p class="text-sm text-gray-700">
    Composition view (NdoHardLink graph and D3) is not wired yet. This placeholder avoids pulling D3 or
    WASM types that are still in flight.
  </p>
</div>
